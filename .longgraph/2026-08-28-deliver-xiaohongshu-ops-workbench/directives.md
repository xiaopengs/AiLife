# 小红书运营工作台 — Directives（监督者 → 执行者，单向）

## Supervisor state（原地更新；执行者忽略）

Last completed tick: none | audited through round: 0 | repo tips: `main` 已整合远程最新实现；本运行待推送的任务契约修订未审计

## STANDING — 权限与边界

S-001 · 执行者可使用获准开发/演示身份和数据完成已登录验收，并可在本地实施由真实消费者路径发现的 MVP 最小修复；每项完成必须在 ledger 记录真实消费者与可重复证据。除本运行文件外，必须保留未知来源的工作树改动。

S-002 · 禁止 reset/stash/clean、删除非本运行文件、生产或其他远程环境修改、真实数据/密钥写入、未声明付费调用，以及以任何非官方方式登录、读取、发布或自动操作小红书账号。

S-003 · PRE-AUTH — 对 R-01 至 R-05 的可复现缺口，若修复同时具备前向兼容、真实消费者、窄回归检查与完整项目门槛，可修改受保护接口、UI 或非破坏性 schema；禁止破坏性迁移、删除既有数据或改变已有公共合同。

S-004 · PRE-AUTH — 项目规则已要求每次任务产物、代码、需求或方案变更均提交到仓库。监督者独立审计完整、门槛为绿的精确写入集合后，可提交并推送该集合；提交前必须重跑匹配的窄门槛和 `git diff --cached --check`。如远程分支领先，先获取并审阅，使用保留双方记录的整合方式处理；发生冲突时不得丢弃远程或本地变更。

S-005 · 不得处理构建分析变量警告或包体建议时擅自引入分析服务、凭据、付费预算或新增产品范围。除非用户另行决定，M4 仅需记录其非阻断原因与后续观察建议。

## Corrections（编号；未折叠的实时队列）

（尚无监督指令。监督者首次审计后仅在发现偏离、接受里程碑或需要停止时追加非重复的紧凑指令包。）
