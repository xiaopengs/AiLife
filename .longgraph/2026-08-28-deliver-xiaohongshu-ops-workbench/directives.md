# 小红书运营工作台 — Directives（监督者 → 执行者，单向）

## Supervisor state（原地更新；执行者忽略）

Last completed tick: none | audited through round: 0 | repo tips: `main` / `origin/main` at run creation; no execution rounds audited

## STANDING — 权限与边界

S-001 · 执行者可在本地完成 MVP 范围内的代码、测试、文档和非破坏性数据迁移；每项完成必须在 ledger 记录真实消费者与可重复证据。除本运行自身文件外，必须保留未知来源的工作树改动。

S-002 · 禁止推送远程仓库、reset/stash/clean、删除非本运行文件、生产或远程环境修改、真实数据/密钥写入、未声明付费调用，以及以任何非官方方式登录、读取、发布或自动操作小红书账号。

S-003 · PRE-AUTH — 对 R-01 至 R-05 必需的新表、列、索引与受保护接口，若同时具备前向迁移、可说明的回退/前进策略、真实消费者和测试证据，可在本地实施并提交；禁止破坏性迁移、删除既有数据或改变已存在公共合同。

S-004 · 每次独立审计确认一个完整、门槛为绿的写入集合后，监督者可仅提交该集合；提交前必须重新运行匹配的窄门槛和 `git diff --cached --check`，提交说明关联轮次或 GAP。任何 push 仍需用户单独授权。

S-005 · 所有需求、方案与代码变更均须更新 `CHANGELOG.md`，并形成 Git 提交；本规则与项目根目录指令一致，不得省略。

## Corrections（编号；未折叠的实时队列）

（尚无监督指令。监督者首次审计后仅在发现偏离、接受里程碑或需要停止时追加非重复的紧凑指令包。）
