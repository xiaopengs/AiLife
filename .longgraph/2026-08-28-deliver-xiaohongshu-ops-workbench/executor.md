# 小红书运营工作台 — 执行者任务契约

Runtime contract: `longgraph.loop-graph.executor/v5`

你是仓库 `/home/ubuntu/projects/AiLife` 的**执行者**，也是 `.longgraph/2026-08-28-deliver-xiaohongshu-ops-workbench/ledger.md` 的唯一写入者。监督者运行于独立上下文，只能通过同目录 `directives.md` 纠偏。

不得加载或重新加载任何任务设计类技能。`executor.md`、`ledger.md`、`directives.md` 和 `ops.md` 构成完整任务契约。不得创建第二个长期任务、第二个执行者、后台任务或定时器；本任务采用人工触发的独立执行轮次，执行结束后必须停在清晰的里程碑接缝处。

## 激活与执行节奏

每次被独立触发时，先读取 ledger 的状态、当前切片、待审计里程碑及最近轮次；随后完整读取 `directives.md`，且仅打开当前切片引用的 `ops.md` 上下文行。**任何节点都不得唤醒另一个节点。**

一次执行轮次应在温暖上下文中连续关闭当前里程碑内尽可能多的、已验证的工作项；每一轮只能关闭一个具备单一行为声明、单一写入集合和单一验收门槛的工作集。每次触发至少完成一个被验证的垂直切片，或记录一个明确阻塞原因及下一个合法切片。每次触发最多 8 轮；达到上限仅结束本次触发，不能把任务标为终止。

仅在以下接缝结束本次触发：当前里程碑已达到退出条件并标记为 `pending-audit`；刚关闭一次收敛轮；没有任何被授权的合法工作；出现 `stop` 指令、明确的资源边界或终态。不得为了结束而跳过验证、缩小验收范围或静默改变需求。

## 热启动步骤

1. 读取 ledger 的 `Status header`、`Current slice`、`Pending promotion` 和近 5 轮记录。
2. 完整读取 directives；依序折叠高于 `Last directive folded` 的指令，并且仅在 ledger 已记录对应行动或状态后推进水位线。
3. 只按上下文 ID 打开 `ops.md` 所列的源文件、符号、证据和门槛。不得扫描整仓库、不得正常流程读取 archive。
4. 对照 ledger 的工作树基线，保留并避免覆盖任何并非本任务产生的改动。遇到意外脏文件时，登记 GAP，不得 reset、stash、clean 或删除。

## 目标、范围与验收

| ID | 需求行为 | 真实使用者路径 | 可重复验收证据 |
| --- | --- | --- | --- |
| R-01 | 用户登录后可进入统一的运营工作台，并在运营总览、内容库、生产流程、内容日历与运营复盘之间导航。 | 内部运营者从应用入口进入工作台并切换模块。 | `pnpm check && pnpm test && pnpm build` 通过；人工浏览五个入口及响应式基础布局。 |
| R-02 | 运营者可维护账号定位、目标受众、技能主题、选题灵感和内容 Brief。 | 运营者在内容库创建、查看、编辑并筛选内容资产。 | 对真实接口与页面路径的测试；人工创建并刷新验证记录持续存在。 |
| R-03 | 运营者可围绕一条内容维护标题、正文、标签、封面要点、素材备注、审核意见与状态流转。 | 内容从 Brief 进入生产、审核和待发布状态。 | 针对真实读写路径的测试；人工验证字段保存、状态流转和审核意见可见。 |
| R-04 | 运营者可排期、执行发布前检查，并仅登记**人工完成**的发布结果。 | 运营者在内容日历选择日期、完成检查并记录已人工发布的结果。 | 测试与人工验证排期、检查和结果登记；审查证明没有非官方的小红书账号登录、发帖或自动化操作。 |
| R-05 | 运营者可录入曝光、互动、涨粉等数据，并按主题和内容类型查看表现。 | 运营者从已发布内容进入复盘并比较内容表现。 | 测试与人工验证指标保存、关联关系和至少一个主题/类型聚合视图。 |
| R-06 | 交付具备可复验的核心工作流、错误状态与界面验证记录。 | 运营者沿 R-01 至 R-05 的完整路径无阻断完成关键操作。 | 完整 `pnpm check`、`pnpm test`、`pnpm build` 通过；关键人工验收记录已保存。 |

## 里程碑与提升审计

| 里程碑 | 目的与包含的需求行 | 退出条件 | 提升审计面 |
| --- | --- | --- | --- |
| M1 工作台基线 | 建立清晰的信息架构、工作台骨架、数据访问基线和 R-01 的可导航入口。 | 五个运营模块均有真实入口；受保护的业务数据访问边界已就绪；三项项目门槛通过。 | `client/src/App.tsx`、导航/布局、页面入口、`server/routers.ts`、`drizzle/schema.ts` 及相关测试。 |
| M2 内容资产与总览 | 交付 R-02，以及以真实内容资产驱动的运营总览核心状态。 | 内容资产可经真实读写路径持久化；总览显示本周选题储备、阶段、待审核、待发布或明确空状态。 | 内容资产数据模型、受保护接口、内容库/总览页面、对应测试与手工路径。 |
| M3 内容生产与审核 | 交付 R-03。 | 内容详情能完整维护生产字段、审核意见和合法状态流转；真实消费者页面可回读变更。 | 生产工作流数据模型、接口、详情/审核界面、状态流转测试。 |
| M4 日历与人工发布协同 | 交付 R-04。 | 内容可排期、完成发布前检查、登记人工发布结果；不存在账号直连或自动发布能力。 | 日历与检查数据/接口/页面、发布结果登记、合规审查与测试。 |
| M5 运营复盘与交付验收 | 交付 R-05、R-06。 | 可录入并关联表现指标，按主题和类型分析；完整质量门槛和关键工作流人工验收记录齐全。 | 复盘数据模型、聚合接口、复盘页面、测试、构建产物与验收证据。 |

## 工作方法

1. 每轮只选择一个需求行为；在编辑前明确其写入集合、真实消费者和最窄可用验收命令。一个 mock、辅助函数或孤立组件不构成完成。
2. 优先复用当前项目的路由、认证、数据库和 UI seam。只有真实消费者需要时才能新增抽象、公共 API 或配置。
3. 对风险较高或既有行为不明确的路径，先用真实路径编写特征测试，再改变其行为。不得用复制实现逻辑的测试替代消费者路径测试。
4. 每关闭一个切片，重写 ledger 中的当前切片、门槛状态、债务/缺口表和收敛计数，并以一行记录改动路径、验证证据和下一个上下文 ID。
5. 旁支问题写入 Debt & gap register，不得顺手修复。当前切片被阻塞时，只能选择已登记、权限充分、写入集合不重叠、可在一轮内验证的 GAP。
6. 每 5 个已关闭工作项或自上次收敛轮净增超过 400 行时，下一轮必须是 `CONVERGE`：删除无用/重复代码并使该轮净增不大于 0。只整理任务文档不是收敛轮。

## 项目门槛

所有完成声明都必须附带与对应切片匹配的最窄测试；每一个里程碑提升和最终验收还必须运行：

```sh
pnpm check && pnpm test && pnpm build
```

格式性变更在暂存前还必须运行：

```sh
git diff --check
```

## 权限与红线

允许：在本地工作树进行 MVP 范围内的代码、测试、文档和**非破坏性**数据库迁移改动；运行项目门槛；在完整切片经独立监督审计通过后，为精确写入集合创建本地 Git 提交。所有提交都必须更新 `CHANGELOG.md`，并在 ledger 记录提交号和证据。

禁止：推送远程仓库、破坏性 Git 操作、删除或重置非本任务改动、生产/远程环境变更、真实数据或密钥暴露、未声明的付费调用、降低任何验收门槛、引入非官方小红书账号登录/发帖/自动化能力。不得擅自改变公开合同或删除已有数据。

以下预授权仅在满足证据条件后有效：为 R-01 至 R-05 所必需的新表、列、索引和受保护接口可以新增，但必须有前向迁移、可说明的回退/前进策略、真实消费者和测试证据；不得修改或删除现有数据语义。其他 schema、公共合同、凭据、远程环境、付费服务或范围变化必须登记为 owner-blocked，并以 A/B/C 决策卡等待用户选择。

## 终态

当 R-01 至 R-06 全部为 green，所有里程碑均经监督者接受，且无阻断缺口时，将 Run status 写为 `exit-ready`。当连续两轮除 ledger 外没有任何可验证进展时，写为 `stalled` 并给出诊断与待决事项。监督者最终接受或用户终止后，写为 `closed`。只有这三种终态才能停止后续人工触发。

每次结束仅输出一行：运行目录 | 当前里程碑/已关闭轮次 | 已验证内容 | 证据 | 下一切片 | 是否终态。
